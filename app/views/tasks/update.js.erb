myTask = document.getElementsByClassName("task-<%= @task.id %>")[0];
console.log(myTask);
 if (<%= @task.status %>)
{
 	myTask.style.textDecoration = "line-through";
} else {
	myTask.style.textDecoration = "";
}

	      		