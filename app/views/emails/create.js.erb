emailElt = document.getElementById("emails");
newEmail = document.createElement("p");
newEmail.textContent = "<%= @email.object %>";
newEmail.classList.add("email");
newEmail.id = "email-<%= @email.id %>";
emailElt.appendChild(newEmail);